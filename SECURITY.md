# 提示词注入防护

本项目把简历和岗位 JD 视为不可信数据。防护目标是阻止数据内容改变模型任务、套取系统提示词或密钥，以及限制异常模型输出影响前端和服务器。

## 已实施的防护

### 1. 服务端输入边界

- 职业领域和具体方向使用服务端职业树校验，拒绝伪造或跨级组合。
- 简历、JD、上传文件和请求频率均有限制。
- API Key 仅存在服务端环境变量中，不发送到浏览器。
- 上传文件只在内存中解析，不落盘保存。

### 2. 文本规范化

- 使用 Unicode NFKC 规范化文本。
- 删除零宽字符、双向文本控制符和不可见控制字符。
- 在检测前统一换行，降低字符混淆和隐藏指令绕过风险。

### 3. 高置信注入扫描

- 检测中英文的“忽略既有指令”“泄露系统提示词”“输出密钥”等组合指令。
- 检测“撇开约束”“别管上文”“最高优先级文本”等同义和委婉表达。
- 检测 XML、Markdown 标题、方括号和 JSON 形式伪造的 system、developer、assistant、tool 角色。
- 对空格、点号、横线等拆分字符生成紧凑扫描文本，识别分词绕过。
- 对 URL 编码和文本中的 Base64 候选片段做单层低成本解码后再次扫描。
- 检测强制修改 score、summary、strengths 等输出字段及 JSON 结构污染指令。
- 检测越狱或角色覆盖表达。
- 命中后直接返回 400，不调用模型，也不消耗 AI 生成额度。
- 安全日志只记录规则编号，不记录用户简历或 JD 原文。

### 4. 模型指令隔离

- 系统消息明确声明简历和 JD 是数据而不是指令。
- 用户数据使用 JSON 字段封装，与任务说明分离。
- 数据结束后再次重申安全边界和唯一允许的任务。
- 模型没有工具、文件系统、数据库或外部操作权限，只能生成文本 JSON。

### 5. 泄漏检测

- 每次 AI 请求生成不同的随机安全标记，只放在系统消息中。
- 如果模型输出包含该标记，服务端判定可能发生系统消息泄漏并丢弃结果。
- 同时检查系统提示中的高辨识度片段，覆盖未包含随机标记的部分复述。
- 上游 API 错误响应不会原样返回用户或写入详细业务日志。

### 6. 输出白名单

- 模型必须返回 JSON，Markdown 或无效 JSON 会被拒绝。
- 服务端只保留评分、摘要、优势、缺口、关键词、改写、ATS 建议、求职信、行动清单和简历草稿字段。
- 数组数量、单项长度、经历数量和文本长度均有上限。
- 未知字段和错误类型会被丢弃。
- 前端使用文本节点或 HTML 转义渲染模型内容，并启用内容安全策略。

### 7. 滥用与成本控制

- 分析和文件上传接口分别限流。
- JSON 请求、简历、JD 和上传文件均有大小上限。
- AI 生成使用内存任务队列，默认限制为 2 个并发和 20 个排队任务。
- 分析创建请求使用幂等标识，网络重试不会重复创建 AI 任务。
- 默认单 IP 每 10 分钟最多创建 10 个任务，并设置可配置的单进程每日任务安全上限。
- AI 请求设置单次超时；只对超时、429 和上游 5xx 等临时错误自动重试。
- 任务查询需要独立随机访问令牌，任务 ID 本身不能读取报告。
- 原始简历和 JD 在任务完成、失败或取消后立即从任务内存清除，结果默认 15 分钟后过期。
- 匿名统计只记录状态、耗时和 token，不记录简历、JD 或模型输出。
- 管理统计接口需要至少 24 位独立令牌，未配置时接口关闭。

### 8. 上线配置保护

- 生产环境缺少有效 AI 服务商配置时拒绝启动，除非明确允许演示模式。
- 自定义 AI 服务商在生产环境必须使用 HTTPS。
- 隐私政策明确披露 AI 服务商处理、服务器任务保留和浏览器本地历史。
- 用户提交分析前必须主动同意隐私政策和用户协议。

## 自动化验证

运行：

```bash
npm test
npm run check
npm run security
```

测试覆盖正常简历、中英文注入、零宽字符绕过、伪造角色标签、Markdown/JSON 边界逃逸、Base64/URL 编码、强制输出字段、系统提示片段泄漏、控制字符清理和模型输出白名单。

## 已知边界

提示词注入无法做到理论上的百分之百识别。自然语言可以无限改写，高置信规则也可能产生漏报或误报。因此：

- 不给模型配置任何能修改数据、发送消息或执行代码的工具权限。
- 持续记录匿名规则命中次数，根据真实攻击样本补充测试。
- 若未来接入 Agent、搜索、数据库或自动投递，必须为每个外部动作增加独立授权和参数校验，不能沿用当前纯文本生成的风险假设。
- 公开上线后应定期轮换 API Key，并设置供应商额度告警。
